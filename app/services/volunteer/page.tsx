import React from 'react';
import Header from 'app/Header';

export const metadata = {
  title: 'Volunteer',
};

const page = () => {
  return (
    <>
      <Header title="Volunteer" />
    </>
  );
};

export default page;
